[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "homebrew"]
# Target platforms to build apps for (Rust target-triple syntax)
#
# Windows (x86_64-pc-windows-msvc) is intentionally excluded: src/player.rs
# uses tokio::net::UnixStream for mpv IPC and libc::kill for orphan-process
# detection, neither of which compile/work on Windows. See
# docs/plans/20260815-windows-support.md for the prerequisite fix before adding it back.
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-unknown-linux-musl"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Whether to install an updater program
install-updater = false
# Whether to publish the Homebrew formula to a tap repository
tap = "qubmen/homebrew-trovers"
# Publish jobs to run in CI
publish-jobs = ["homebrew"]
